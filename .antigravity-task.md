# Task: Nexus Task 22ab4a1e-5ef3-4bda-8a50-3a46d407adc9

Execute Nexus task 22ab4a1e-5ef3-4bda-8a50-3a46d407adc9. Check the task details in TheNexus dashboard for full context.

---
## Completion Instructions
When done, send a POST request to notify the orchestrator:

```bash
curl -X POST http://127.0.0.1:54322/callback \
  -H "Content-Type: application/json" \
  -d '{"taskId": "gc-1777889680210", "nexusProjectId": "", "nexusTaskId": "22ab4a1e-5ef3-4bda-8a50-3a46d407adc9", "conversationId": "<extract your conversation ID from the path of your brain directory>", "result": "<summary of what you did>"}'
```

## If You Cannot Complete This Task
If you encounter an error you cannot resolve, you MUST still send the callback.
Include `"failed": true` and describe the error in the result:

```bash
curl -X POST http://127.0.0.1:54322/callback \
  -H "Content-Type: application/json" \
  -d '{"taskId": "gc-1777889680210", "nexusProjectId": "", "nexusTaskId": "22ab4a1e-5ef3-4bda-8a50-3a46d407adc9", "conversationId": "<extract your conversation ID from the path of your brain directory>", "result": "<what went wrong>", "failed": true}'
```
**The callback is critical** — without it, Praxis cannot track this task's outcome.
