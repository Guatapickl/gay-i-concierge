# Gay I Club live release — September 6, 2026

Live: https://gayiclub.com. Released for Robert’s review, per his explicit instruction to deploy rather than review a local server.

- Released application source: `2652829` on `codex/relaunch-2026-09-06`; GitHub main and canonical checkout synchronized.
- Firebase App Hosting: `gayiclub-web`, `us-east4`, project `gayiclub`.
- Final rollout: `build-2026-09-06-003`, serving 100% of traffic. Confirmed live around 9:21 AM Eastern.
- Matching Firestore privacy rules compiled and released successfully.
- No DNS changes or member announcements.
- Prior production rollback revision: `gayiclub-web-build-2026-09-04-003`; pre-launch source tag retained.

## Live verification

All checks below were run against the actual public site after the final release:

- Apex200; www308 to apex; HTTP301 to HTTPS.
- Legal pages, sign-in/sign-up, robots, sitemap and security.txt200.
- Signed-out Events redirects to the public sign-in URL, never the internal Firebase hostname.
- Public apex has no noindex header; Firebase platform hostname remains noindex.
- Security headers present; social image 1200×630.
- Same-origin invalid session input400, hostile origin403.
- One disposable Firebase test account signed in successfully, obtained a secure session cookie, and reached the protected profile page200.
- Authenticated directory200 with no email, phone, consent or beta preference fields exposed.
- Cross-member private profile read403.
- Exact temporary Auth user `launch-qa-20260906-live` deleted immediately after the checks. No profile or event fixture added to production.
- Desktop/mobile screenshots saved; no mobile horizontal overflow.
- 54 tests pass, production build passes. Independent reviewer cleared the allowlisted Firebase proxy-host fix (12 targeted tests plus TypeScript).

Evidence: `evidence/live-routes.json`, `live-auth-check.json`, `live-traffic.json`, `deploy-live.log`, `deploy-rules.log`, `live-desktop.png`, `live-mobile.png`, `tests-final.log`, `build.log`.

## Deployment correction

The first live deployment exposed Firebase’s internal hostname in session checks and redirects, and incorrectly applied preview noindex to the apex. A shared helper now accepts only the known apex, www, and exact Firebase platform hostname from forwarded headers. It corrects session-origin comparison, login redirects, www canonical redirects and indexing. An intermediate deployment was cancelled before serving so the incomplete fix was not promoted. Final live checks above confirm the correction.

## Remaining follow-ups

Robert intentionally chose live review despite the earlier incomplete readiness checklist. Shared support receipts/registry and cross-project beta syncing are still unbuilt; Praxis feedback routing needs configuration, and actual application email flows need inbox verification. Earlier broader QA gaps remain recorded. These do not imply the release was unapproved; they are follow-up work and are not marked falsely passed.

See `robert-checklist-2026-09-06.md` for Robert’s actions. No local test server is needed for review.

Private pre/post-release Firestore/Auth-record/DNS backups remain outside deployment at `/Users/robertwashko/Documents/ChatGPT/GayIClub/private-backups/2026-09-06`. Post-release snapshot has 8 Auth users, matching the pre-release count after disposable-account cleanup. Full password restore/disaster recovery has not been proven.
