# Gay I Club release and rollback — 2026-09-06

Status: release authorized and executed for live owner review on September6. Final release evidence is recorded in release-2026-09-06.md. No DNS mutation or member announcement. Robert explicitly superseded the prelaunch-only approval workflow below.

## Current state and backups

- Production: https://gayiclub.com; Firebase App Hosting backend gayiclub-web, us-east4, project gayiclub.
- HTTPS apex and www custom domains: HOST_ACTIVE, OWNERSHIP_ACTIVE, CERT_ACTIVE confirmed through the App Hosting API on September 6.
- Cloudflare authoritative nameservers: arturo.ns.cloudflare.com and elly.ns.cloudflare.com. Apex A: 35.219.200.206.
- Pre-change source: git tag `pre-launch-2026-09-06`, commit `b66ee21` in the canonical repository.
- Private backups (not committed/published): `/Users/robertwashko/Documents/ChatGPT/GayIClub/private-backups/2026-09-06/` contains recursive Firestore document snapshot, Firebase user records, DNS records, hosting backend/domains/rollouts, and public homepage/robots snapshot. File JSON was read back; a restoration drill has not been run. Firebase user JSON is an administrative record export; password hash preservation/import must be independently checked before claiming complete credential restoration.
- Before screenshots: `evidence/before-desktop.png`, `evidence/before-mobile.png`.
- Old sitemap: HTTP 404. No old traffic report is available in this task; preserve existing routes.

## Forward

1. Resolve blocking readiness rows and record Robert’s approval of the final report/build. Record actual release time in Eastern.
2. Confirm the final branch commit, full tests, real project build and independent review correspond. Refresh backups if live data changed materially.
3. Confirm the trusted Firebase credential is active without printing it. The generic Firebase CLI backend list failed; direct Admin-authenticated App Hosting API reads succeeded. Use the scoped service credential when running the reviewed deploy commands; verify deployment rights first.
4. Deploy the matching application and tightened Firestore rules as one coordinated release. These changes depend on one another: the new member directory reads a safe server API, and the new rules deny raw cross-member profile reads. Review staging first; do not leave old client queries paired with the new rules during normal traffic.
5. Existing configured source deployment command is `firebase deploy --only apphosting --project gayiclub`. Rules/indexes use `firebase deploy --only firestore:rules,firestore:indexes --project gayiclub`. Do not redeploy unrelated functions unless changed.
6. The release sets the www-to-apex redirect, security headers and public SEO files in application code. **No DNS records currently need changing.** If hosting asks for a different record, record old/new values, TTL and proxy state here and obtain exact-record approval before applying it.
7. Add the prepared Gay I Club feedback-to-Nexus mapping during a coordinated Praxis restart (see support-mapping-handoff.md), preserving all other mappings. Verify routing before promising support automation.

## Rollback

- Application: use Firebase App Hosting’s retained previous successful rollout. The private `rollouts.json` records the prior successful September 4 builds; capture the backend’s active rollout immediately before release and select that exact rollout for rollback.
- Local source recovery: the original worktree and pre-launch tag remain intact. Rebuild/redeploy the pre-launch tag only after confirming the corresponding data/rules contract.
- Firestore rules: retain the exported/previous source rules, but do not silently reintroduce the known cross-member private-data access defect. If application rollback would require unsafe rules, restrict member access while correcting the safe directory path and tell Robert. No destructive data migration is part of this release.
- DNS: no change planned. If one is approved later, use the exact private DNS export to write its reverse record changes before applying the forward changes.
- Trigger rollback or containment for failed sign-in, 5xx responses, private-data exposure, blank pages/CSP failures, broken member directory, or Robert’s request.

## Verify after release

- Confirm the platform rollout reports success and corresponds to the approved build.
- Request apex, www and HTTP; verify valid certificate, canonical redirect, expected security headers and actual new content.
- Confirm /robots.txt is plaintext and public, /sitemap.xml lists public production URLs, legal pages are available, and preview hosts remain noindex.
- Complete sign-in/sign-out, profile, directory isolation, RSVP/poll/agenda and controlled inbox flows on the real domain; check exact test records and archive/remove only those IDs through an authorized cleanup procedure.
- Confirm widget loads and routes correctly; do not submit member details for testing.
- Capture desktop/mobile after screenshots, post-release database and DNS backups; tag the released commit `live-2026-09-06`.
- Record the actual launch in the Praxis vault and project. Only then send an authorized member announcement.
